<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RemoteEverything" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_bg</item>
        <!-- The web view applies the page's own dark styles; never force-invert it. -->
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
